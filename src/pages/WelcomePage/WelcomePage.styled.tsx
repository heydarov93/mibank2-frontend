import { Box, styled } from '@mui/material';

export const StyledContainer = styled(Box)(() => ({
  display: 'flex',
  alignItems: 'center',
  gap: '48px',
  paddingInline: '24px',
  paddingTop: '16px',
}));
