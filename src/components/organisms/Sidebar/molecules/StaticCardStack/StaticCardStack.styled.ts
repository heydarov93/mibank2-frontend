import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

export const StyledStaticCard = styled(Box)(() => ({
  position: 'absolute',
  width: '100%',
  height: '100%',
  borderRadius: 'inherit',
}));
