import { Icon, styled } from '@mui/material';

export const StyledIcon = styled(Icon)(() => ({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  width: '40px',
  height: '40px',
  borderRadius: '50%',
}));
